# 全排列路径+损失销量最小化补货路径优化思路

## 1. 问题描述

对于 m 个待补货点位，考虑所有可能的补货顺序（即 m! 条路径），结合每条路径下每个点位的补货完成时间，评估因补货不及时造成的销量损失，选择损失最小的路径作为最优补货顺序。

---

## 2. 主要流程与规则

### 2.1 路径生成

- 对 m 个点位生成所有全排列路径（共 m! 条）。

### 2.2 路径模拟与到达时间计算

对每一条路径，依次模拟补货过程，计算每个点位的补货完成时间，规则如下：

1. **出发时间递推**  
   - 第一个点位的出发时间为车辆实际出发时间（如当天早上7点）。
   - 后续点位的出发时间 = 上一个点位的到达时间 + 上一个点位的补货耗时。

2. **特殊事件处理**  
   - **库存不足需回央厨（CK）补货**  
     - 若补货后车辆剩余库存 < n，则需回CK补货，后续所有点位的到达时间 +3(?) 小时。
   - **临近交班需回交班点**  
     - 若补货后接近交班时间（如早9点、晚9点），需回交班点，后续所有点位的到达时间 +1(?) 小时。
   - **两事件重叠处理**  
     - 若回CK的时间接近交班时间，仅加3小时，不叠加为4小时。
   - -**休息时间**  
     - 待确认

3. **补货时间窗口约束**  
   - 若某点位的到达时间不在其允许的补货时间窗口内，则该路径无效，直接跳过。

### 2.3 损失销量评估

- 对于每条有效路径，计算每个点位因补货不及时造成的销量损失，累加得到该路径的总损失销量。

---

## 3. 公式与伪代码


### 3.1 损失销量计算

- 设 \( t_{stockout}[i] \) 为点位 i 的预计缺货时间
- 设 \( sales\_rate[i] \) 为点位 i 的单位时间销量预测
- 若 \( t_{arrive}[i] > t_{stockout}[i] \)，则损失销量为 \( (t_{arrive}[i] - t_{stockout}[i]) \times sales\_rate[i] \)
- 否则损失为0

总损失销量：
\[
Loss(P) = \sum_{i=1}^{m} \max(0, t_{arrive}[i] - t_{stockout}[i]) \times sales\_rate[i]
\]

### 3.2 路径筛选与最优解

- 对所有有效路径，计算总损失销量，选择损失最小的路径作为最优解。

---

## 4. 伪代码示例

```python
# 中文注释
# 输入: m个点位，车辆出发时间，补货耗时，库存阈值n，交班时间，补货时间窗口，点位销量预测等
best_loss = float('inf')
best_path = None

for path in 全排列(点位列表):
    t = 车辆出发时间
    truck_stock = 初始库存
    valid = True
    total_loss = 0

    for i, point in enumerate(path):
        t += 路径上一个点到当前点的行驶时间
        if t 不在 point.补货时间窗口:
            valid = False
            break

        # 计算损失销量
        if t > point.预计缺货时间:
            total_loss += (t - point.预计缺货时间) * point.销量预测

        # 补货
        truck_stock -= point.补货量
        t += point.补货耗时

        # 检查是否需要回CK
        if truck_stock < n:
            t += 3小时
            truck_stock = 补满

        # 检查是否需要回交班点
        if t 接近交班时间:
            t += 1小时

        # 两事件重叠只加3小时

    if valid and total_loss < best_loss:
        best_loss = total_loss
        best_path = path

# 输出: best_path, best_loss
```

---

## 5. 说明与补充

- 路径数量为 m!，当 m 较大时需考虑剪枝（如按当前库存-未来 T 时间窗口的销量倒序筛选），否则计算量极大。
- 关于出发时间，需综合考虑回库、交班、休息、补货等多种影响因素，这些因素会对路径规划的准确性产生一定影响。但根据实际业务分析，行驶时间约占总耗时的 34%，因此在前期建模和优化时，可暂不将这些因素作为关键影响变量，后续可根据实际需求逐步引入更精细的时间约束。
- 未来销量损失的预估需精确到小时级别：在实际应用中，销量损失不再是简单地判断某个时间段是否缺货，而是需要对每个小时的销量进行精细化预测，并将各时段的损失进行累加，形成定量分析, 精度要求高
- 跨周末的情况未细化考虑
- sku 的缺货影响未细化, 折算到盒菜的数量预估
- 可根据业务实际，进一步细化损失计算方式和特殊规则。
