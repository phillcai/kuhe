
1. 问题描述

对于 m 个待补货点位，考虑所有可能的补货顺序（即 m! 条路径），结合每条路径下每个点位的补货完成时间，评估因补货不及时造成的销量损失，选择损失最小的路径作为最优补货顺序。


---

2. 主要流程与规则

2.1 路径生成

- 对 m 个点位生成所有全排列路径（共 m! 条）。
2.2 路径模拟与到达时间计算

对每一条路径，依次模拟补货过程，计算每个点位的补货完成时间，规则如下：

1. 出发时间递推  
  - 第一个点位的出发时间为车辆实际出发时间（如当天早上7点）。
  - 后续点位的出发时间 = 上一个点位的到达时间 + 上一个点位的补货耗时。
2. 特殊事件处理  
  - 库存不足需回央厨（CK）补货  
    - 回 ck 的点位数是可预估的, 可以把 ck 加到路径规划, 在 ck 等待时间为 1 小时
  - 临近交班需回交班点  
    - 回 交班的点位数是可预估的, 可以把 交班加到路径规划, 在 交班等待时间为 0.5小时
  - 休息时间 
    - 待确认
  
3. 补货时间窗口约束  
  - 若某点位的到达时间不在其允许的补货时间窗口内，则该点位跳过,  计入销量损失且继续计算该路径的后续点位. (考量:  不在补货窗口把该路径跳过本质是对有窗口限制点位的加权, 需要谨慎权衡). 销量损失的为(该路径结束时间后最近的补货窗口开始时间 - 缺货时间) * 销量预测
2.3 损失销量评估

- 对于每条有效路径，计算每个点位因补货不及时造成的销量损失，累加得到该路径的总损失销量。

---

1. 公式与伪代码


3.1 损失销量计算

- 设 \( t_{stockout}[i] \) 为点位 i 的预计缺货时间
- 设 \( sales\_rate[i] \) 为点位 i 的单位时间销量预测
- 若 \( t_{arrive}[i] > t_{stockout}[i] \)，则损失销量为 \( (t_{arrive}[i] - t_{stockout}[i]) \times sales\_rate[i] \)
- 否则损失为0
总损失销量：
\[
Loss(P) = \sum_{i=1}^{m} \max(0, t_{arrive}[i] - t_{stockout}[i]) \times sales\_rate[i]
\]

3.2 路径筛选与最优解

- 对所有有效路径，计算总损失销量，选择损失最小的路径作为最优解。

---

4. 伪代码示例

# 中文注释：全排列路径+损失销量最小化补货路径优化伪代码（CK和交班点动态插入）

# 输入参数说明：
# points: 待补货点位列表，每个点包含补货窗口、预计缺货时间、销量预测、补货耗时、补货量等信息
# depart_time: 车辆实际出发时间
# drive_time(i, j): 点位i到点位j的行驶时间
# init_stock: 车辆初始库存
# stock_threshold: 触发回CK补货的库存阈值
# shift_time: 交班时间点

best_loss = float('inf')  # 最优损失销量初始化为无穷大
best_path = None          # 最优路径初始化为空

# 遍历所有点位的全排列路径
for path in 全排列(points):
    t = depart_time       # 当前时间初始化为出发时间
    stock = init_stock   # 当前车辆库存
    total_loss = 0       # 当前路径累计损失销量
    valid = True         # 路径有效性标志
    real_path = []       # 实际执行路径（含动态插入的CK/交班点）

    for idx, point in enumerate(path):
        

        # 判断是否需要回CK补货
        if stock < min_stock or sku < min_sku:
            # 动态插入CK点
            ck_point = CK点对象  
            t += drive_time(real_path[-1], ck_point) if real_path else drive_time('start', ck_point)
            t += ck_point.replenish_time
            stock = init_stock
            real_path.append(ck_point)
            # 回来后再去当前点
            t += drive_time(ck_point, point)

        # 判断是否需要交班
        if t  > shift_time:
            # 动态插入交班点
            shift_point = 交班点对象  
            t += drive_time(real_path[-1], shift_point) if real_path else drive_time('start', shift_point)
            t += shift_point.replenish_time
            real_path.append(shift_point)
            # 回来后再去当前点
            t += drive_time(shift_point, point)
      
        # 计算到达当前点的时间
        if idx == 0:
            t += drive_time('start', point)
        else:
            t += drive_time(real_path[-1], point)

        # 检查是否在补货时间窗口内, point.replenish_amount小于最小补货数同样逻辑处理
        if not (point.window[0] <= t <= point.window[1]):
            # 不在窗口内，计入损失，跳过补货，继续下一个点
           
            total_loss += (24H 窗口- point.stockout_time) * point.sales_rate
            continue

        # 计算因补货不及时造成的销量损失
        if t > point.stockout_time:
            total_loss += (t - point.stockout_time) * point.sales_rate

        # 执行补货操作
        stock -= point.replenish_amount
        t += point.replenish_time
        real_path.append(point)

        

    # 路径有效且损失更小则更新最优解
    if  total_loss < best_loss:
        best_loss = total_loss
        best_path = real_path

# 输出最优路径和最小损失销量
return best_path, best_loss
讨论点 :
  - 由于剪枝算法权重关系, 区域内最高销量的点位召回概率高, 特别是大点位临近午高峰时, 可以考虑加个最少补货数(Y)限制, 若预估补货数小于 Y, 则等同不在补货窗口处理.

---

5. 算法优化与评价

### 5.1 说明与补充

- 路径数量为 m!，当 m 较大时需考虑剪枝，否则计算量极大
  - 排除未来 12 小时没有补货窗口的
  - 当前Sku >=8 且 stock>= 65 时 排除
  - 按优先级算法选 top 8
- 关于出发时间，需综合考虑回库、交班、休息、补货等多种影响因素，这些因素会对路径规划的准确性产生一定影响。但根据实际业务分析，行驶时间约占总耗时的 34%，因此在前期建模和优化时，可暂不将这些因素作为关键影响变量，后续可根据实际需求逐步引入更精细的时间约束。
- 未来销量损失的预估需精确到小时级别：在实际应用中，销量损失不再是简单地判断某个时间段是否缺货，而是需要对每个小时的销量进行精细化预测，并将各时段的损失进行累加，形成定量分析, 精度要求高
- 跨周末的情况未细化考虑
- sku 的缺货影响未细化, 折算到盒菜的数量预估
- 可根据业务实际，进一步细化损失计算方式和特殊规则。
- 兜底: 召回数为0 时用全局召回(大中点位), 到最近的点位

### 5.2 智能配送路径优化评价指标设计

#### 5.2.1 问题背景

**当前评价方式的问题**
现有的路径优化算法在选择最优路径时，主要存在以下问题：

1. **评价指标单一**：仅考虑销量损失（TotalLoss）和时间（TotalDuration）
2. **评价逻辑简单**：采用优先级比较，先比较损失，损失相同时再比较时间
3. **缺乏综合考量**：没有考虑距离、补货效率等其他重要因素
4. **权重固定**：无法根据业务需求调整不同指标的重要性
5. **缺乏约束处理**：没有对超出业务阈值的路径进行惩罚

**业务需求分析**
智能配送系统需要综合考虑以下因素：

- **销量损失最小化**：直接影响业务收益，是最重要的指标
- **时间效率优化**：影响配送效率和车辆利用率
- **补货效率提升**：影响整体配送效果  

#### 5.2.2 综合评价指标体系

**核心评价指标**

1. **销量损失评分（LossScore）**
   - 指标：TotalLoss（总销量损失）
   - 目标：损失越小越好
   - 权重：（最重要）

2. **时间效率评分（TimeScore）**
   - 指标：TotalDuration（总耗时）
   - 目标：时间越短越好

3. **补货效率评分（ReplenishScore）**
   - 指标：实际补货点位数量/总点位数量
   - 目标：补货点位越多越好

**业务约束指标**

1. **最大可接受损失阈值**： 具体数据待后期数据统计给出
2. **最大可接受时间阈值**： 具体数据待后期数据统计给出
3. **惩罚机制**：超出阈值的路径将受到评分惩罚

#### 5.2.3 评价算法设计

**归一化评分法**

采用Min-Max归一化方法，将所有指标统一到0-1范围：

```go
// 对于越小越好的指标（损失、时间）
Score = (max - value) / (max - min)

// 对于越大越好的指标（补货效率）
Score = (value - min) / (max - min)
```

**综合评分公式**

```go
TotalScore = w1 * LossScore + w2 * TimeScore + w3 * ReplenishScore

其中：
w1 = 0.6 (销量损失权重)
w2 = 0.25 (时间效率权重)
w3 = 0.15 (补货效率权重)
```

**惩罚机制**

1. **损失惩罚**：超过MaxAcceptableLoss时
2. **时间惩罚**：超过MaxAcceptableTime时
