# 点位分拣逻辑 SQL 说明文档

## 1. 查询目标

本 SQL 主要用于**计算某个点位（point）在特定条件下各主食商品（commodity）的补货需求量**，并结合库存、中央厨房库存等信息，输出每个商品的最终补货数量和类型。

---

## 2. 顶层 SELECT 说明

```sql
SELECT  
    a.commodity_id AS commodity_id,
    CASE 
        WHEN coalesce(a.main_food_cnt,0) > coalesce(qty,0) 
        THEN coalesce(qty,0)  
        ELSE coalesce(a.main_food_cnt,0) 
    END main_food_cnt,
    CASE 
        WHEN coalesce(a.main_food_cnt,0) > coalesce(qty,0) 
        THEN 1  
        ELSE 0 
    END main_food_type
FROM ...
```

- **commodity_id**：商品ID
- **main_food_cnt**：最终主食补货数量（取点位需求和中央厨房库存的较小值）
- **main_food_type**：补货类型（1=受中央厨房库存限制，0=按点位需求）

---

## 3. a 子查询（点位主食需求量计算）

### 3.1 计算每个商品的主食需求量

```sql
SELECT  
    a.point_id,
    a.commodity_id,
    CASE 
        WHEN main_food_cnt-coalesce(b.commodity_stock,0) > 0 
        THEN main_food_cnt-coalesce(b.commodity_stock,0)  
        ELSE 0 
    END main_food_cnt
FROM ...
```

- **main_food_cnt**：点位主食需求量减去点位现有库存后的实际需求（如为负则记为0）

---

### 3.2 计算点位主食需求量（main_food_cnt）

```sql
SELECT  
    a.point_id,
    b.commodity_id,
    CASE 
        WHEN point_amount <= 30 
        THEN if (round(point_amount * (b.dish_score/ point_dish_score),0) < 1,1,round(point_amount * (b.dish_score/ point_dish_score),0) )  
        ELSE if(round(point_amount * (b.dish_score/ point_dish_score)/3,0)*3 <= 3,3,round(point_amount * (b.dish_score/ point_dish_score)/3,0)*3) 
    END main_food_cnt
FROM ...
```

- **point_amount <= 30**：小点位，按比例分配，最少1份
- **point_amount > 30**：大点位，按比例分配，结果向下取整到3的倍数，最少3份

---

### 3.3 计算点位主食总需求量（point_amount）

```sql
SELECT  
    a.point_id,
    point_max_stock,
    if(point_amount < 30,30,if(point_amount >= point_max_stock,point_max_stock,point_amount)) point_amount
FROM ...
```

- **point_amount < 30**：最少30份
- **point_amount >= point_max_stock**：不超过最大库存
- **否则**：按实际需求

---

### 3.4 计算点位实际需求量（point_amount）

```sql
SELECT  
    a.point_id,
    CASE 
        WHEN b.point_id is not null 
        THEN a.point_max_stock  
        ELSE a.point_amount 
    END point_amount,
    point_max_stock
FROM ...
```

- **如有全补单**（b.point_id不为空）：直接补满最大库存
- **否则**：按实际需求

---

### 3.5 计算点位需求量（point_amount）

```sql
SELECT  
    a.point_id,
    CASE 
        WHEN DATEDIFF(current_date,open_date) <= 7 
        THEN point_max_stock
        WHEN coalesce(5_day_main_food_cnt,avg_cnt_5day) > point_max_stock 
        THEN point_max_stock  
        ELSE coalesce(5_day_main_food_cnt,avg_cnt_5day) 
    END point_amount,
    point_max_stock
FROM ...
```

- **新开点（7天内）**：补满最大库存
- **5天主食销量或均值大于最大库存**：补满最大库存
- **否则**：按5天主食销量或均值

---

### 3.6 计算点位最大库存（point_max_stock）和开业日期（open_date）

```sql
SELECT  
    a.id point_id,
    CASE 
        WHEN device_type = 1 THEN 108
        WHEN a.id = 55 THEN 72  
        ELSE 96 
    END point_max_stock,
    CASE 
        WHEN min_dt is null THEN current_date  
        ELSE min_dt 
    END open_date
FROM ...
```

- **不同设备类型/点位ID**：最大库存不同
- **开业日期**：取订单最早日期或当前日期

---

### 3.7 5天主食销量统计（5_day_main_food_cnt）

```sql
SELECT  
    point_id,
    CASE 
        WHEN point_size = 3 THEN round(coalesce(5_day_main_food_cnt,0)*1.0,0)  
        ELSE round(coalesce(5_day_main_food_cnt,0),0) 
    END 5_day_main_food_cnt
FROM ...
```

- **不同点位规格**：销量统计方式略有不同

---

### 3.8 5天主食销量明细

```sql
SELECT  
    point_id,
    point_size,
    round(SUM(case WHEN rn <= 119 THEN main_food_cnt end),0) 5_day_main_food_cnt
FROM ...
GROUP BY  point_id, point_size
```

- **统计近5天（120小时）主食销量**

---

### 3.9 近7天主食销量均值（avg_cnt_5day）

```sql
SELECT  
    a.point_id,
    coalesce(a.main_food_cnt,b.main_food_cnt,50) avg_cnt_5day
FROM ...
```

- **优先取点位规格主食均值，否则取实际销量，否则默认50**

---

### 3.10 点位现有主食库存（commodity_stock）

```sql
SELECT  
    point_id,
    commodity_id,
    SUM(abs(amount))-SUM(abs(locked_amount)) commodity_stock
FROM smart_cooker_sg.inventory_point
WHERE commodity_id IN (主食商品)
AND amount > 0
AND point_id = ?
GROUP BY  point_id, commodity_id
```

- **点位主食商品的可用库存**

---

## 4. b 子查询（中央厨房库存）

```sql
SELECT  
    commodity_id,
    SUM(qty) qty
FROM smart_cooker_sg.central_kitchen_frame_inventory
WHERE frame_id IN (SELECT distinct frame_id FROM smart_cooker_sg.central_kitchen_car_frame WHERE state = 2 AND car_id = ? )
AND commodity_type = 1
AND qty > 0
GROUP BY  commodity_id
```

- **统计中央厨房指定车辆上主食商品的总库存**

---

## 5. 业务流程总结

1. **确定点位最大库存和需求量**（考虑新开点、历史销量、全补单等）
2. **按主食分配权重，计算每个商品的需求量**
3. **扣除点位现有库存，得到实际补货需求**
4. **与中央厨房库存对比，最终补货量取较小值**
5. **输出每个商品的补货数量和类型**

---

## 6. 主要参数说明

- `?`：SQL中的问号为参数占位符，分别代表点位ID、商品ID列表、车辆ID等。

---

如需对某一层的 SQL 进行更详细的逐行注释，或需要将其转为 Python 代码实现，请告知！ 