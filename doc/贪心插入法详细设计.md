# 基于优先级的贪心插入法——详细算法设计

## 一、问题建模
- 输入：
  - 点位信息（ID、坐标、最大库存、当前库存、补货时间窗、类型等）
  - 未来销量预测
  - 货车信息（最大载货量、班次、起止点）
- 约束：
  - 每个点位的补货时间窗口
  - 货车最大载货量
  - 每个点位的最大库存
  - 补货优先级（缺货风险、销量、点位类型等）
- 目标：
  - 在满足约束的前提下，优先补货高优先级点位，尽量多补货，路径总耗时最短

## 二、算法主要步骤
1. **数据预处理**
   - 计算每个点位的缺货风险和补货优先级。
   - 过滤掉不需要补货的点位。
2. **点位优先级排序**
   - 按优先级从高到低排序所有待补货点位。
3. **初始化路径**
   - 路径列表为空。
   - 当前车辆剩余容量 = 最大载货量。
   - 当前时间 = 车辆出发时间。
   - 当前车辆位置 = 出发点（如中央厨房）。
4. **贪心插入点位**
   - 遍历排序后的点位列表：
     - 判断该点位是否能在时间窗内补货，且车辆容量足够。
     - 若可行，将其插入当前路径，更新车辆剩余容量、当前时间、当前位置。
     - 若不可行（容量不足或时间窗冲突），则：
       - 若还有未补货点位，结束当前路径，开启新路径（车辆返回出发点补货，重置容量和时间）。
       - 或跳过该点位。
   - 重复直到所有点位处理完毕。
5. **输出结果**
   - 每条路径的补货顺序、预计到达时间。
   - 未能补货的点位及原因。

## 三、详细伪代码

```python
# 中文注释：贪心插入法详细伪代码
输入：点位列表V，车辆最大载货量C，车辆出发时间T0，出发点S
输出：路径列表routes，每条路径为点位访问顺序

def greedy_insertion(V, C, T0, S):
    # 1. 预处理和优先级计算
    for v in V:
        v.priority = calc_priority(v)  # 计算优先级（如缺货风险、销量、点位类型等加权）
    V = [v for v in V if need_replenish(v)]  # 过滤不需补货点位
    V.sort(key=lambda x: x.priority, reverse=True)  # 按优先级降序排序

    routes = []  # 路径列表
    unvisited = set(V)  # 未访问点位集合

    while unvisited:
        route = []  # 当前路径
        capacity = C  # 当前车辆剩余容量
        time = T0  # 当前时间
        pos = S  # 当前车辆位置
        
        for v in list(unvisited):
            travel_time = calc_travel_time(pos, v)
            arrive_time = time + travel_time
            # 检查时间窗和容量约束
            if arrive_time < v.time_window_start:
                arrive_time = v.time_window_start  # 需等待到时间窗开始
            if (arrive_time > v.time_window_end) or (capacity < v.demand):
                continue  # 时间窗冲突或容量不足，跳过
            # 插入该点位
            route.append((v, arrive_time))
            capacity -= v.demand
            time = arrive_time + v.service_time  # 补货耗时
            pos = v
            unvisited.remove(v)
        # 路径结束，返回出发点
        if route:
            routes.append(route)
        else:
            break  # 无法继续插入点位
    return routes

# 关键辅助函数：
def calc_priority(v):
    # 结合缺货风险、销量、点位类型等因素加权
    return w1*v.stockout_risk + w2*v.sales + w3*v.type_score

def need_replenish(v):
    # 判断点位是否需要补货
    return v.stock < v.replenish_threshold

def calc_travel_time(a, b):
    # 计算两点间行驶时间（可用距离/速度或地图API）
    return distance(a, b) / avg_speed
```

## 四、实现要点说明
- 优先级计算可根据实际业务灵活调整权重。
- 时间窗、容量等约束需在插入前严格判断。
- 可扩展为多车多区域场景，每辆车独立执行上述流程。
- 可输出未能补货点位及原因（如时间窗冲突、容量不足等）。
- 路径可用于可视化展示。

## 五、优先级计算公式详细说明

### 1. 优先级总公式

priority = w1 * 缺货风险分 + w2 * 预计销量分 + w3 * 点位类型分

- priority：点位补货优先级得分，分数越高优先级越高。
- w1, w2, w3：权重参数，满足 w1 + w2 + w3 = 1。

### 2. 各项分值计算与归一化

#### （1）缺货风险分（stockout_score）
- 计算方式：
  - 先预测每个点位的预计缺货时间（单位：小时），如 v.estimated_stockout_hours。
  - 用倒数归一化，缺货越快分数越高。
  - 公式：stockout_score = 1 / (v.estimated_stockout_hours + 1)
  - 说明：加1防止分母为0。

#### （2）预计销量分（sales_score）
- 计算方式：
  - 预测补货周期内的销量，如 v.predicted_sales。
  - 用最大销量归一化。
  - 公式：sales_score = v.predicted_sales / max_predicted_sales
  - 说明：max_predicted_sales为所有点位中最大预测销量。

#### （3）点位类型分（type_score）
- 计算方式：
  - 按点位类型赋固定分值。
  - 推荐赋值：
    - 大点位：1.0
    - 新点位：0.8
    - 中点位：0.5
    - 小点位：0.3
    - 特殊点位：可根据实际业务单独设定
  - 公式：type_score = type_score_dict[v.type]

### 3. 推荐权重
- w1 = 0.6（缺货风险最重要）
- w2 = 0.25（预计销量次之）
- w3 = 0.15（点位类型）

### 4. 公式举例
假设某点位：
- 预计缺货时间 = 3小时
- 预测销量 = 50，max_predicted_sales = 100
- 类型为"大点位"

则：
- stockout_score = 1 / (3+1) = 0.25
- sales_score = 50 / 100 = 0.5
- type_score = 1.0
- priority = 0.6*0.25 + 0.25*0.5 + 0.15*1.0 = 0.15 + 0.125 + 0.15 = 0.425

## 六、结合谷歌地图API的最优路径选择实现说明

在贪心插入法的每一轮点位选择时，可结合谷歌地图API进行实际行驶路径的优化，具体实现建议如下：

### 1. 候选点位筛选
- 每轮从未补货点位中，按优先级得分选出前N个（如3或6个）作为候选点位。

### 2. 路径距离获取
- 对于当前车辆位置与每个候选点位，调用Google Maps Directions API，获取实际道路行驶距离或耗时。
- 可批量请求，提升效率。

### 3. 最优点位选择
- 在候选点位中，选择距离（或耗时）最短的点位作为下一个补货目标。
- 若有多个点位距离相同，可优先选优先级更高者。

### 4. 路径更新
- 将选中的点位加入当前路径，更新车辆当前位置和时间。
- 重复上述流程，直到所有点位分配完毕或车辆容量/时间窗受限。

### 5. 伪代码补充
```python
for round in range(max_rounds):
    candidates = get_topN_priority_points(unvisited, N)
    min_dist = float('inf')
    next_point = None
    for v in candidates:
        dist = get_google_maps_distance(pos, v)  # 调用Google Maps API
        if dist < min_dist:
            min_dist = dist
            next_point = v
    # 选择最优点位，加入路径
    ...
```

### 6. 说明
- 该方法兼顾了业务优先级和实际行驶路径的合理性。
- 可根据实际API调用限制和业务需求调整候选点数量N。
- 若需进一步提升整体路径效率，可在候选点基础上引入局部路径优化算法。

--- 