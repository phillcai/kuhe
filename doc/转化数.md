1. 菜品转化率(按SKU权重)
  - 分点位、分session_id计算每个点位，每个菜品的曝光转化率
  - 每个点位取最近28天的曝光转化率
  - 线下、线上每个session同权重，14天session数为每天的session数之和(避免session出现跨天的情况)
曝光转化率计算公式如下：
$$点位菜品曝光转化率=\sum_{\mathclap{1\le i\le 15(双电磁为10)}} \frac{14天内对应商品购买session数}{14天内对应商品曝光session数} *\frac{session对应的曝光sku数}{15(双电磁为10)}$$
2. 菜品转化率计算(sku较高时菜品销售占比)
  - 限制最近两周点位销售菜品以及本周生产菜品，且对应点位绑定对应的菜品
  - 限定最近30天，sku>=14(双电磁为8)时所有对应菜品的销售数量/sku>=14(双电磁为8)时所有菜品的销售数量
  - 未取到数据的菜品，取对应点位取到菜品评分的最小值作为对应评分。
  注：sku>=14(双电磁为8)目前采用小时级别快照数据，服务端从7月中旬开始记录订单级别的sku数，记录满14天后对应数据会切换为订单级别的数据。
3. 菜品转化率计算(Customer Choice Modeling)
  - 服务端每个订单均会记录一条用户选择的菜品以及可选菜品的列表数据。
  - 限制sku>=10(双电磁为sku>=6)
  - 通过最大似然函数求解用户选择每个菜品的概率。
  - 算法开发中
4. 点位补货逻辑
min(点位未来5天销量,点位最大库存)*菜品转化率占比
菜品转化率占比计算公式为：
$$每个菜品转化率占比=\frac{货车上剩余菜品且对应点位可补菜品转化率}{货车上剩余菜品且对应点位可补菜品的转化率之和} $$
菜品转化率有个降级逻辑:
1. 对应点位对应菜品转化率
2. 点位所有菜品转化率均值(新菜或者轮换菜)
3. 同类型点位对应菜品转化率均值(新点位旧菜)
4. 同类型点位所有菜品转化率均值(新点位新菜或者轮换菜)

5. 不同算法菜品分拣比例
总量为7月07日至7月13日所有点位的销量，对比基于权重算出来的菜品占比与基于满sku情况下算出来的销量占比。
[图片]
所有点位
[图片]
Hwa Chong
6. 现有约束条件
- 货车分拣过程中每个盒菜必须分拣为9的倍数，为了补满整个货架，会导致有的菜上取整，有的菜品下取整，菜品分拣比例和预测之间存在偏差。
- 点位分拣过程中，为了提升点位的补满率，限制每个菜盒补完之后为3的整数倍，也会影响菜品的真实菜品分拣比例。
