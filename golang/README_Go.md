# 点位聚类和时间窗口分析 - Go版本

这是原Python脚本的Go语言重写版本，用于将点位按地理位置进行动态分区，并统计每组的时间窗口分布，支持数量均衡和时间窗分散优化。

## 功能特性

1. **CSV数据读取**: 从`data/点位信息.csv`文件读取点位信息
2. **数据过滤**: 自动过滤掉pid为998和999的点位
3. **K-means聚类**: 基于经纬度进行地理聚类
4. **数量均衡**: 确保各组点位数量相对均衡
5. **时间窗口优化**: 对9:00-21:00时间段的点位进行分散优化
6. **离群点处理**: 自动检测并修正地理离群点
7. **孤立点归组**: 将极端孤立的点位分配到最近的分组
8. **结果输出**: 生成详细的分组报告和CSV文件

## 文件结构

```
kuhe/
├── golang/
│   ├── main.go          # 主程序文件
│   ├── go.mod           # Go模块文件
│   ├── README_Go.md     # 说明文档
│   └── kuhe             # 编译后的可执行文件
├── data/
│   └── 点位信息.csv      # 输入数据文件
└── output/
    └── output.csv       # 输出结果文件
```

## 输入数据格式

CSV文件应包含以下列：
- `id`: 点位ID
- `longitude`: 经度
- `latitude`: 纬度
- `start_time`: 开始时间 (HH:MM格式)
- `end_time`: 结束时间 (HH:MM格式)

## 编译和运行

### 1. 确保已安装Go (版本1.21或更高)

```bash
go version
```

### 2. 编译程序

```bash
cd golang
go build -o kuhe main.go
```

### 3. 运行程序

```bash
cd golang
./kuhe
```

或者直接运行：

```bash
cd golang
go run main.go
```

## 输出结果

程序会输出两种格式的分组结果：

1. **详细格式**: 显示每个分组的点位ID和时间窗口信息
2. **简化格式**: 显示每个分组的点位ID列表（逗号分隔）
3. **CSV文件**: 在`output/output.csv`中保存完整的分组结果

## 算法流程

1. **数据预处理**: 读取CSV文件，过滤无效点位，合并同一ID的多个时间窗口
2. **K-means聚类**: 使用经纬度进行3组聚类
3. **数量均衡**: 通过移动边界点实现各组数量均衡
4. **时间窗口优化**: 对9:00-21:00时间段的点位进行分散优化
5. **数量再均衡**: 均衡每组主时间窗点位的数量
6. **离群点处理**: 检测并修正地理离群点
7. **孤立点归组**: 处理极端孤立的点位

## 参数配置

主要参数可以在`main.go`中调整：

- `nClusters`: 聚类组数（默认3）
- `minInterval`: 时间窗口最小间隔（默认120分钟）
- `maxIter`: 离群点处理最大迭代次数（默认5）
- `stdFactor`: 离群点判定标准差倍数（默认2）
- `thresholdKm`: 孤立点归组阈值（默认20公里）

## 性能特点

- **高效**: Go语言的并发特性和编译优化提供更好的性能
- **内存安全**: 强类型系统和垃圾回收机制
- **错误处理**: 完善的错误处理机制
- **可维护性**: 清晰的代码结构和注释

## 与原Python版本的对比

| 特性 | Python版本 | Go版本 |
|------|------------|--------|
| 执行速度 | 较慢 | 更快 |
| 内存使用 | 较高 | 较低 |
| 部署便利性 | 需要Python环境 | 编译后可直接运行 |
| 并发支持 | 需要额外库 | 原生支持 |
| 错误处理 | 异常机制 | 显式错误处理 |

## 注意事项

1. 确保输入CSV文件格式正确
2. 程序会自动创建`output`目录
3. 时间格式必须为HH:MM
4. 经纬度应为有效的数值
5. 程序会跳过格式错误的行并继续处理

## 故障排除

如果遇到问题，请检查：

1. CSV文件是否存在且格式正确
2. 是否有足够的磁盘空间写入输出文件
3. 输入数据是否包含有效的经纬度和时间信息
4. Go版本是否满足要求（1.21+） 