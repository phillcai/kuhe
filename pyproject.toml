[project]
name = "kuhe"
version = "0.1.0"
description = "点位优先级计算项目"
authors = ["your_name <your_email@example.com>"]
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "numpy",
    "folium",
    "pyperclip"
] 